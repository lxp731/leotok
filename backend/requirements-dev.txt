# 本地开发 / 类型检查依赖(运行在 x86_64 开发机上)。
# 手机 Termux 上的运行依赖见 requirements.txt(setup.sh 负责安装,
# 其中含 Android wheel,与本地平台不同,不能在这里直接装)。
#
# 用法:
#   uv venv --python python3.13   # 或 python3 -m venv .venv
#   uv pip install -r requirements-dev.txt
-r requirements.txt

# 代码质量工具
ruff
basedpyright
